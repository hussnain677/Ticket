import { NativeModules, processColor } from 'react-native';
/**
 * Type of option used in JS side
 */

const {
  RNCColorPicker
} = NativeModules;
const ColorPicker = {
  showColorPicker: (options, callback) => {
    const convertedOptions = { ...options,
      initialColor: (options === null || options === void 0 ? void 0 : options.initialColor) ? processColor(options.initialColor) : undefined
    };
    RNCColorPicker.showColorPicker(convertedOptions, callback ? callback : () => {});
  }
};
export default ColorPicker;
//# sourceMappingURL=index.js.map